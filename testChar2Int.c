#include <stdio.h>

int main()
{
/*   char c='17';
   int num= 0;
   printf("c is %c, num is %d", c, num);
   num= c - '0';
   printf("After casting, c is %c, num is %d", c, c);

*/





/* 
char name[]=""; //the declaration of a C-String.

fgets(name,100,stdin);	//input. 
printf("The name is: %s", name);


return 0; 

}
